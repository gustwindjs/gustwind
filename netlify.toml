[functions]
  included_files = ["netlify/functions/layouts/**"]

[build]
  base    = ""
  publish = "build"
  command = "curl -fsSL https://deno.land/x/install/install.sh | sh -s v1.38.2 && /opt/buildhome/.deno/bin/deno task netlify:build"
