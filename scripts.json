{
  "allow": ["env", "read", "write", "net", "run"],
  "env": {
    "NO_COLOR": 1
  },
  "scripts": {
    "start": {
      "cmd": "deno run --no-check --unstable src/serve.ts",
      "watch": true
    },
    "build": {
      "cmd": "deno run --no-check --unstable src/build.ts"
    },
    "watch:build": {
      "cmd": "deno run --no-check --unstable src/build.ts",
      "watch": true
    }
  },
  "imap": "import_map.json"
}
