export default () => {
  // TODO: Parse query and push it to generate
  // TODO: Trigger and cache generate
  return new Response("og image goes here");
};

export const config = { path: "/api/og" };
